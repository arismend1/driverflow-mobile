diff --git a/src/screens/ForgotPasswordScreen.tsx b/src/screens/ForgotPasswordScreen.tsx
index 7e3cd0d..75c451e 100644
--- a/src/screens/ForgotPasswordScreen.tsx
+++ b/src/screens/ForgotPasswordScreen.tsx
@@ -16,10 +16,15 @@ export default function ForgotPasswordScreen() {
 
         setLoading(true);
         try {
-            await recoverPassword(email);
+            const res = await recoverPassword(email);
+            if (!res.ok) {
+                Alert.alert('Error', res.error || 'Unable to process request');
+                return;
+            }
+            
             Alert.alert(
                 'Request Sent',
-                'If the email is registered, you will receive instructions and a temporary password shortly.',
+                'A password reset link has been sent to your email.',
                 [{ text: 'OK', onPress: () => navigation.goBack() }]
             );
         } catch (error: any) {
@@ -33,7 +38,7 @@ export default function ForgotPasswordScreen() {
         <View style={styles.container}>
             <View style={styles.header}>
                 <Text style={styles.title}>Recover Password</Text>
-                <Text style={styles.subtitle}>Enter your email to receive a temporary password.</Text>
+                <Text style={styles.subtitle}>Enter your email to receive a password reset link.</Text>
             </View>
 
             <View style={styles.form}>
